;; Mutation-Based Equivalence Benchmark
;; Source:     AutomatArk / instance13447.smt2
;; Mutations:  literal_char_inc, range_shrink_hi, literal_char_inc
;; Status:     sat
;;
;; Original regex vs mutated regex
;; Equivalence check: unsat ⟺ L(R1) = L(R2)

(set-info :smt-lib-version 2.6)
(set-info :category "mutation")
(set-info :status sat)
(set-logic QF_S)


;; R1: original
;; R2: mutated (literal_char_inc, range_shrink_hi, literal_char_inc)
(assert
  (not
    (=
      (re.++ (re.union (str.to_re " ") (str.to_re "_") (re.range "0" "9") (re.range "a" "z") (re.range "A" "Z") (str.to_re "$") (str.to_re "%") (str.to_re "'") (str.to_re "-") (str.to_re "@") (str.to_re "{") (str.to_re "}") (str.to_re "~") (str.to_re "!") (str.to_re "#") (str.to_re "(") (str.to_re ")") (str.to_re "&") (str.to_re "^")) (str.to_re "\u{a}"))
      (re.++ (re.union (str.to_re " ") (str.to_re "`") (re.range "0" "8") (re.range "a" "z") (re.range "A" "Z") (str.to_re "$") (str.to_re "%") (str.to_re "'") (str.to_re "-") (str.to_re "@") (str.to_re "{") (str.to_re "}") (str.to_re "~") (str.to_re "!") (str.to_re "#") (str.to_re ")") (str.to_re ")") (str.to_re "&") (str.to_re "^")) (str.to_re "\u{a}")))))

(check-sat)
(exit)
